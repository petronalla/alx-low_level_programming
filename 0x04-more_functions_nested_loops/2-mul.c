#include "main.h"

/**
 * mul - Multiplies two integers
 *
 * @a: First integer.
 *
 * @b: Second integer.
 *
 * Return: Returns the product.
 */
int mul(int a, int b)
{
	int multy;

	multy = (a * b);
	return (multy);
}